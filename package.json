{
  "name": "public-service-grievance-system-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace runner for JanSeva / DIGIT CMS Public Service Delivery & Citizen Grievance Management System",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm start",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  }
}
